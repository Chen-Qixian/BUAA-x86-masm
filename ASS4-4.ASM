STACK 	SEGMENT	PARA	STACK
		DW		100H DUP(?)
STACK	ENDS

DATA	SEGMENT	PARA
	NUM 	DW 	1606H,1160H,1234H,0FFFFH
	DIVISOR DW 	1000H,100H,10H,1H
DATA 	ENDS

CODE 	SEGMENT PARA
		ASSUME	CS:CODE,DS:DATA,SS:STACK

OUTPUT 	PROC
	PRINT:
		MOV 	SI,OFFSET DIVISOR
		MOV 	CX,4
	OUTPUT_LP:
		XOR 	DX,DX
		DIV 	WORD PTR [SI]
		PUSH 	DX
		CMP 	AL,10
		JB		OUTPUT_CONTINUE
		ADD 	AL,7
	OUTPUT_CONTINUE:
		ADD 	AL,30H
		MOV 	DL,AL
		MOV 	AH,2
		INT 	21H
		INC 	SI
		INC 	SI
		POP 	AX
		LOOP 	OUTPUT_LP
		RET
OUTPUT 	ENDP

MAIN	PROC 	FAR
	MAINPROC:
		MOV 	AX,DATA
		MOV 	DS,AX
		MOV 	ES,AX

		MOV 	AX,NUM
		CALL 	OUTPUT

		MOV 	AX,NUM+2
		CALL 	OUTPUT

		MOV 	AX,NUM+4
		CALL 	OUTPUT

		MOV 	AX,NUM+6
		CALL 	OUTPUT

	EXIT:	
		MOV 	AX,4C00H
		INT 	21H
MAIN 	ENDP

CODE 	ENDS
		END 	MAIN