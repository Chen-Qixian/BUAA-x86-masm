STACK	SEGMENT	PARA STACK
		DW		100H DUP(?)
STACK	ENDS

DATA 	SEGMENT PARA
VALUE 	DW 		1606H
DIVISOR DW 		10
DATA	ENDS

CODE 	SEGMENT PARA
		ASSUME	CS:CODE,DS:DATA,SS:STACK
MAIN	PROC 	FAR
		MOV 	AX,DATA
		MOV 	DS,AX

		MOV 	AX,VALUE
		MOV 	CX,0
LP1:	XOR 	DX,DX
		DIV 	WORD PTR DIVISOR
		OR 		DL,30H
		PUSH 	DX
		INC 	CX
		CMP 	AX,0
		JA 		LP1

LP2: 	POP 	DX
		MOV 	AH,2
		INT 	21H
		LOOP 	LP2

EXIT:	MOV 	AX,4C00H
		INT 	21H
MAIN 	ENDP

CODE 	ENDS
		END 	MAIN