STACK	SEGMENT	PARA STACK
		DW		100H DUP(?)
STACK	ENDS

DATA	SEGMENT	PARA
X 		DW		2
Y		DW		3
Z		DW 		0
DIVISOR DW 		10
DATA	ENDS

CODE	SEGMENT	PARA
ASSUME	CS:CODE,DS:DATA,SS:STACK
MAIN	PROC	FAR
		MOV 	AX,DATA
		MOV 	DS,AX

		MOV 	AX,X
		MOV		BX,Y
		MUL 	BX

		MOV 	Z,AX

		CALL 	P1

EXIT:	MOV		AX,4C00H
		INT		21H
MAIN	ENDP

P1 		PROC
		MOV 	AX,Z
		MOV 	CX,0
LP1:	XOR 	DX,DX
		DIV 	WORD PTR DIVISOR
		OR 		DL,30H
		PUSH 	DX
		INC 	CX
		CMP 	AX,0
		JA 		LP1

LP2: 	POP 	DX
		MOV 	AH,2
		INT 	21H
		LOOP 	LP2
		RET
P1 		ENDP

CODE	ENDS
		END 	MAIN