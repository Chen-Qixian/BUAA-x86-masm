STACK	SEGMENT	PARA STACK
		DW		100H DUP(?)
STACK	ENDS

DATA	SEGMENT	PARA
X 		DW		1606H
Y		DW		1160H
DIVISOR	DW		1000,100,10,1
DATA	ENDS

CODE	SEGMENT	PARA
ASSUME	CS:CODE,DS:DATA,SS:STACK
MAIN	PROC	FAR
		MOV 	AX,DATA
		MOV 	DS,AX

		MOV		CX,4
		MOV		AX,X
		MOV		SI,OFFSET DIVISOR

LP1:	XOR		DX,DX
		DIV 	WORD PTR[SI]

		PUSH	DX
		OR		AL,30H

		MOV		DL,AL
		MOV		AH,2
		INT		21H
		INC 	SI
		INC		SI
		POP		AX
		LOOP	LP1

		MOV		CX,4
		MOV		AX,Y
		MOV		SI,OFFSET DIVISOR

LP2:	XOR		DX,DX
		DIV 	WORD PTR[SI]

		PUSH	DX
		OR		AL,30H

		MOV		DL,AL
		MOV		AH,2
		INT		21H
		INC 	SI
		INC		SI
		POP		AX
		LOOP	LP2

EXIT:	MOV		AX,4C00H
		INT		21H
MAIN	ENDP
CODE	ENDS
		END 	MAIN